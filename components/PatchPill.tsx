// Patch version is displayed in the nav now; keeping the constant exported
// so any page can reference it, and the empty component for back-compat.
export const CURRENT_PATCH = "4.7.1";

export function PatchPill() {
  return null;
}
